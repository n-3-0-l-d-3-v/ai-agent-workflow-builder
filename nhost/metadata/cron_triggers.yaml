- name: dispatch_scheduled_workflows
  webhook: '{{NHOST_FUNCTIONS_URL}}/cron/dispatchScheduled'
  schedule: '* * * * *'
  include_in_metadata: true
  payload: {}
  retry_conf:
    num_retries: 1
    retry_interval_seconds: 10
    timeout_seconds: 30
    tolerance_seconds: 21600
  headers:
    - name: x-hasura-admin-secret
      value_from_env: NHOST_ADMIN_SECRET
  comment: Polls workflow_triggers (type=scheduled) every minute and starts matching runs
